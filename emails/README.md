# Email Templates

Runtime email templates are stored in the database as `MessageTemplate` records and seeded from `prisma/seed.ts`.

This folder is reserved for future shared email partials or exported production templates.
